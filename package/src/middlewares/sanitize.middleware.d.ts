/**
 * Filter properties from req.body by comparing an object
 * @param classModel any
 * @param body any
 */
declare const sanitizeBody: (classModel: any, body: any) => any;
export default sanitizeBody;
